<!--
  This is the Liquid code for a reusable content block.
  Save this as a file named `sections/content-block.liquid` in your theme.

  This single section can be used for both your "Project Management" and "Work Together"
  sections by simply changing the settings in the Theme Editor.
-->

{% schema %}
{
  "name": "Content Block",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Project"
    },
    {
      "type": "text",
      "id": "highlighted_text",
      "label": "Highlighted Text",
      "default": "Management"
    },
    {
      "type": "richtext",
      "id": "text",
      "label": "Text Content",
      "default": "<p>Images, videos, PDFs and audio files are supported. Create math expressions and diagrams directly from the app. Take photos with the mobile app and save them to a note.</p>"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button Label",
      "default": "Get Started"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button Link"
    },
    {
      "type": "image_picker",
      "id": "block_image",
      "label": "Block Image"
    },
    {
      "type": "select",
      "id": "layout",
      "label": "Layout",
      "options": [
        { "value": "text_left", "label": "Text on Left" },
        { "value": "text_right", "label": "Text on Right" }
      ],
      "default": "text_left"
    }
  ],
  "presets": [
    {
      "name": "Project Management Section",
      "category": "Custom Sections",
      "settings": {
        "layout": "text_left",
        "heading": "Project",
        "highlighted_text": "Management",
        "text": "<p>Images, videos, PDFs and audio files are supported. Create math expressions and diagrams directly from the app. Take photos with the mobile app and save them to a note.</p>",
        "button_label": "Get Started"
      }
    },
    {
      "name": "Work Together Section",
      "category": "Custom Sections",
      "settings": {
        "layout": "text_right",
        "heading": "Work",
        "highlighted_text": "together",
        "text": "<p>With whitepace, share your notes with your colleagues and collaborate on them. You can also publish a note to the internet and share the URL with others.</p>",
        "button_label": "Try it now"
      }
    }
  ]
}
{% endschema %}

<!--
  The HTML below uses a conditional class to change the grid layout based on the
  "layout" setting. This makes it a single, reusable section.
-->
<section class="content-block {% if section.settings.layout == 'text_right' %}content-block-reverse{% endif %}">
  <div class="container content-grid">
    <div class="content-text">
      <h2>
        {{ section.settings.heading | split: section.settings.highlighted_text | first }}
        <span class="highlight">
          {{ section.settings.highlighted_text }}
          <div class="underline"></div>
        </span>
      </h2>
      {{ section.settings.text }}
      {% if section.settings.button_label != blank and section.settings.button_link != blank %}
        <a href="{{ section.settings.button_link }}" class="btn btn-blue">
          {{ section.settings.button_label }}
          <img src="https://placehold.co/14x14/FFFFFF/000000?text=→" alt="Right Arrow" />
        </a>
      {% endif %}
    </div>
    <div class="content-image">
      {% if section.settings.block_image != blank %}
        <img src="{{ section.settings.block_image | image_url: width: 800 }}" alt="{{ section.settings.block_image.alt }}" />
      {% else %}
        <div class="image-placeholder"></div>
      {% endif %}
    </div>
  </div>
</section>

<!--
  This is the CSS for both the project management and work together sections.
  It's a direct copy of your original styles, with class names changed to be generic
  and reusable. It's important to keep all the media queries to ensure responsiveness.
-->
<style>
  :root {
    --blue: #043873;
    --blue-light: #4f9cf9;
    --yellow: #ffe492;
    --white: #ffffff;
  }
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
  }
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    border-radius: 8px;
    border: none;
    font-size: 18px;
  }
  .btn img {
    width: 14px;
    height: 14px;
    vertical-align: middle;
    margin-left: 0;
  }
  .btn-blue {
    background-color: var(--blue-light);
    color: var(--white);
    width: 219px;
    height: 63px;
    padding: 20px;
  }
  .content-block {
    background-color: var(--white);
    padding: 80px 0;
    position: relative;
    overflow: hidden;
  }
  .content-block::before {
    content: "";
    position: absolute;
    left: 0;
    bottom: 0;
    width: 602.84px;
    height: 448.52px;
    background: url('https://placehold.co/602x448/C4DEFd/043873?text=Project+Background') left bottom/contain no-repeat;
    z-index: 0;
  }
  .content-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    align-items: center;
    gap: 40px;
    position: relative;
    z-index: 1;
  }
  .content-text h2 {
    font-size: 72px;
    font-weight: 700;
    line-height: 1.2;
    margin-bottom: 20px;
    color: #212529;
  }
  .highlight {
    position: relative;
    display: inline-block;
  }
  .underline {
    position: absolute;
    left: 0;
    bottom: 0;
    width: 678.74px; /* Default for Project Management */
    height: 65.55px; /* Default for Project Management */
    background-image: url('https://placehold.co/678x65/FFE492/FFE492');
    background-size: 100% 100%;
    background-repeat: no-repeat;
    pointer-events: none;
    z-index: -1;
    display: block;
    margin: 0;
  }
  .content-text p {
    font-size: 18px;
    color: #212529;
    margin-bottom: 30px;
    line-height: 1.6;
  }
  .content-image {
    width: 100%;
    max-width: 748px;
    height: auto;
    aspect-ratio: 748 / 547;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .content-image img {
    max-width: 100%;
    height: auto;
  }
  .image-placeholder {
    width: 100%;
    aspect-ratio: 748 / 547;
    border-radius: 8px;
    background-color: #c4defd;
  }

  .content-block-reverse .content-grid {
    gap: 60px;
  }
  .content-block-reverse .content-image {
    order: 2;
  }
  .content-block-reverse .content-text {
    order: 1;
  }

  /* Responsive */
  @media (min-width: 1920px) {
    .container {
      max-width: 1700px;
    }
  }
  @media (min-width: 1440px) {
    .container {
      max-width: 1400px;
    }
  }
  @media (max-width: 1240px) {
    .container {
      padding: 0 30px;
    }
  }
  @media (max-width: 968px) {
    .content-text h2 {
      font-size: 56px;
    }
    .content-grid {
      gap: 30px;
    }
    .content-block-reverse .content-grid {
      gap: 30px;
    }
  }
  @media (max-width: 768px) {
    .content-grid {
      grid-template-columns: 1fr;
      text-align: center;
    }
    .content-image {
      order: 2;
      aspect-ratio: unset;
      height: 250px;
    }
    .content-text {
      order: 1;
    }
    .content-block-reverse .content-grid {
      grid-template-columns: 1fr;
    }
    .content-block-reverse .content-image {
      order: 2;
    }
    .content-block-reverse .content-text {
      order: 1;
    }
    .content-text h2 {
      font-size: 42px;
    }
    .content-text p {
      font-size: 16px;
    }
  }
  @media (max-width: 480px) {
    .container {
      padding: 0 15px;
    }
    .content-block {
      padding: 40px 0;
    }
    .btn {
      width: 65%;
      height: 70px;
      padding: 20px 40px;
      margin-bottom: 30px;
    }
    .content-text h2 {
      font-size: 36px;
      font-weight: 700;
      line-height: 100%;
      text-align: center;
      margin-bottom: 30px;
    }
    .content-text p {
      font-size: 18px;
      line-height: 30px;
      text-align: center;
      margin-bottom: 40px;
    }
    .underline {
      width: 80vw;
      max-width: none;
      height: 36px;
      bottom: -5px;
    }
    .content-image {
      margin-bottom: 32px;
      margin-top: 8px;
    }
  }
</style>